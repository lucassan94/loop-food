# 01. Pendências — Projeto: Migração Asaas → API da Rede

> Última atualização: 02/08/2026 — Migração **implementada** (Fases 0-12 concluídas), **deploy via SSH realizado** (stack `restaurante-v3` no ar) e **estrutura de pastas reorganizada** (`app/`, `spec/`, `project-manager/`, `others/`, `migrations/`, `trash/`). Abaixo somente o que está **pendente**.

## ✅ Ações executadas (02/08/2026)

1. ✅ **Deploy via SSH** — novo fluxo sem GitHub: `deploy.py` (paramiko/SFTP) sobe os arquivos para `/opt/restaurante-v3` e gerencia a stack com `docker-compose` (v1.29.2 no servidor). Containers: `restaurante-v3_backend` (8090), `_cliente` (8091), `_router` (8094) — todos **healthy**. Stack antiga `loop-food-temp` removida (portas liberadas).
2. ✅ **Healthcheck do backend corrigido** — wget agora usa `127.0.0.1` (IPv4); o `localhost` resolvia para `::1` e o servidor só escuta em IPv4 → falso `unhealthy` eliminado.
3. ✅ **Uploads em bind mount** — `./backend/uploads:/app/uploads`; as 44 imagens do cardápio foram enviadas junto e **servidas com 200** em produção (fim do BUG-011 também em produção).
4. ✅ **Volumes órfãos removidos** (`loop-food-dev_*`, `loop-food-temp_backend_uploads`).
5. ✅ **.env de produção criado** no servidor (não sobrescreve edições futuras).
6. ✅ **Certificado wildcard `*.loopautomacoes.com.br` emitido** (Let's Encrypt via DNS-01 manual na Umbler) — resolveu a página em branco dos subdomínios de tenant (`ERR_CERT_COMMON_NAME_INVALID`): o cert anterior só cobria o domínio raiz e a CSP `upgrade-insecure-requests` forçava HTTPS. Validade até 31/10/2026. **502 transitório** durante o reload do NPM (proxy temporariamente usou `forward_scheme=https` contra o router HTTP) — voltou ao normal sozinho após o reload.
7. ✅ **Deploy incremental no `deploy.py`** — sincronização por manifesto SHA-256 (`.deploy_manifest.json`) no servidor: 1ª subida envia tudo e grava o manifesto; nas seguintes só o que mudou. Flags `--force`/`--dry-run`; `.vite` (cache dev) excluído. **Testado na VPS real**: upload 124 → 0/124; images 45 → 0/45; manifesto com 169 entradas (124 código + 45 imagens).

## ⏳ Pendências ativas

1. **Credenciais reais do sandbox da Rede** (PV + Chave de Integração + projeto no Portal developer.userede.com.br) — preencher no módulo god → testar as jornadas (e2e-rede-test.js). ⛔ **Bloqueado:** usuário ainda não tem acesso ao portal.
2. **Cadastro da URL de notificação PIX** — via call center da Rede (4001 4433 / 0800 728 4433), ativação ~2 dias úteis. Sem isso, PIX só é confirmado por polling.
3. **Habilitar TLS 1.2+ nos nginx** (bloco HTTPS já adicionado/comentado — MELHORIA-001): montar certificados (volumes no docker-compose) e terminar TLS no proxy externo — necessário para PCI-DSS do checkout transparente.
4. **Tenant 3 (Loop) sem usuários** (BUG-010) — criar admin no módulo god se o tenant ainda estiver em uso.
5. **Trocar o `JWT_SECRET` global** (hoje `dev-secret-change-in-production`) por um segredo aleatório no `.env` do servidor — os tenants 1-3 têm `jwt_secret` próprio no banco, então o global é apenas fallback; troca não invalida sessões atuais.
6. **Segurança do deploy**: trocar a senha da VPS (foi compartilhada) e migrar para **chave SSH** no `deploy.py` (o paramiko já suporta `key_filename`).
7. **Migrations**: rodar `python deploy.py migrate` quando novas migrations forem adicionadas (banco atual já tem 001-027).
8. ⚠️ **Renovação automática do certificado wildcard** — o `renewal/npm-1.conf` agora usa `authenticator = manual` + `pref_challs = dns-01` (hooks `/tmp/acme-auth.sh`/`/tmp/acme-cleanup.sh`), então o NPM ao renovar vai **pausar pedindo novo registro TXT** na Umbler. Expira em **31/10/2026**. Antes disso: renovar manualmente (repetir o procedimento DNS-01) ou trocar para um plugin DNS automático. **Se expirar, a página em branco volta.**
9. **Concluir movimentação pós-reorganização** — com os dev servers fechados: mover os resíduos de `cliente/`, `entregador/`, `restaurante/` (raiz) para `app/` (`src/`, `public/`, `.vite/`). O `backend/` e `router/` da raiz já estão vazios (conteúdo movido). Depois remover as pastas vazias da raiz.

---

### 🔍 Pendências levantadas pela execução de testes (03/08/2026)

10. **Deployar o fix do CEP (`AppError`)** — `app/backend/src/services/cep.js` local já lança `AppError` (400 `INVALID_CEP` / 404 `CEP_NOT_FOUND` / 400 `CEP_LOOKUP_FAILED`), mas o deploy ainda retorna **500** para CEP inexistente (API-05). Rodar o deploy do backend (deploy.py) e revalidar `POST /api/cep` com `00000000` → deve voltar 400/404.
11. **Reaplicar seeds no tenant 1** — os usuários seed `cliente`/`cliente123` e `entregador`/`entregador123` NÃO existem no deploy (banco limpo sem re-seed). Reaplicar `seed.js` (ou criar via signup/admin). Até lá, as suítes usam usuários persistentes de teste (cliente `(11) 97000-7777`, entregador id 7 `(11) 97000-8888`).
12. **Decidir sobre a feature Salão** — o deploy está com `features: {salao: false, delivery: true}` → KDS/PDV/Mesas não aparecem no painel (RS-17/18 não aplicáveis). Confirmar se é intencional ou se o `ConfigView` precisa religar.
13. **Investigar bucket global do rate limit de login via nginx (BUG-014)** — verificar propagação de `X-Forwarded-For` no router/nginx e o `trust proxy` do backend; o tráfego UI parece compartilhar um único bucket (5 tentativas/15min) entre todos os clientes. Impacto: um atacante (ou automação) pode bloquear logins de todos os usuários.
14. **Limpeza dos dados de teste gerados pelas suítes** — pedidos de teste (`Teste RS Aceitar`, `Cliente EN Fila/Cancel/Realtime`), categorias `Cat UI *` (ficaram no banco), usuário de teste (cliente id 42, entregador id 7 com senha resetada), raios (removidos). Rodar `limpar-dados-teste.js` ou limpeza manual antes de um go-live.

---

### 🔄 Mudança de deploy: GitHub → Portainer (03/08/2026)

15. ✅ **Volta ao deploy via GitHub (Portainer)** — compose canônico movido para a **raiz** do repo (`docker-compose.yml` + `stack.env` commitado), contextos `./app/*`, `name: restaurante-v3`. O `app/docker-compose.yml` foi removido. O `deploy.py` virou fallback (sincroniza `app/` → `{dir}/app` + compose/stack.env da raiz).
16. ⚠️ **Migração de uploads p/ volume nomeado (1ª vez no Portainer)** — o novo compose usa `backend_uploads:/app/uploads` (volume nomeado) em vez do bind mount antigo `./backend/uploads`. Se havia imagens no bind mount, copiar antes de derrubar a stack antiga:
    ```bash
    docker run --rm -v restaurante-v3_backend_uploads:/dest -v /opt/restaurante-v3/backend/uploads:/src alpine sh -c 'cp -a /src/. /dest/'
    ```
17. ⏳ **Configurar a stack `restaurante-v3` no Portainer** (http://86.48.18.22:9000) — Git repository: `https://github.com/lucassan94/loop-food.git` (branch `main`, compose path `docker-compose.yml`), env vars via `stack.env`. Depois testar Pull & redeploy + verificar healthcheck dos 3 serviços.
18. ✅ **Push notifications: VAPID agora configurados** — o `.env` do servidor NÃO tinha as chaves VAPID (push estava quebrado em produção); o `stack.env` commitado na raiz inclui `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` (iguais às do `others/stack.env`), então o push volta a funcionar no deploy via Portainer.
19. 🧹 **Remover pastas planas órfãs no servidor** (se o fallback SSH for usado): após o switch para `{dir}/app`, os `{dir}/backend`, `{dir}/cliente`, `{dir}/entregador`, `{dir}/restaurante`, `{dir}/router` antigos ficam órfãos — remover manualmente para liberar disco.
20. 🖼️ **Imagens no banco (migration 028)** — nova tabela `imagens` (BYTEA) centraliza todos os uploads no Postgres; URLs `/uploads/...` intactas; volume `backend_uploads` removido do compose. Ordem de aplicação no deploy: rodar a migration 028 → `node src/migrar-imagens-para-banco.js` (importa disco + base64 antigos) → subir a stack. Após confirmar, o `UPLOAD_DIR` em disco e o `deploy.py images` ficam obsoletos.
21. 🔐 **Role app_user (migration 029, BUG-016) — APLICADA no banco; falta ativar no runtime** — role não-superuser criada em produção + WITH CHECK em todas as policies de isolamento + RLS em `banners/categorias/pagamentos/raios_entrega`. Para ativar: (a) definir **override `DB_PASS`** (senha do app_user, gerada e salva em `/tmp/app_user_pass.txt` no servidor) na stack do Portainer (`stack.env` commitado tem `DB_PASS` vazio — repo é **PÚBLICO**, nunca commitar a senha); (b) redeploy via Portainer → backend conecta como `app_user` (RLS ativo). `migrate.js`/seeds/scripts usam `DB_ADMIN_USER=default`/`DB_ADMIN_PASS=default` (override no Portainer).
22. 🔑 **Rotacionar a senha do superuser `default`** — hoje `default/default` (pública via compose). Após o switch para `app_user`, alterar: `ALTER ROLE default PASSWORD '...'` e definir `DB_ADMIN_PASS` só no override do Portainer (nunca no repo).
23. 📦 **`DB_PASS` no fluxo fallback SSH** — `deploy.py env` gera `{dir}/.env` com `DB_PASS` vazio (fail-closed). Se o fallback for usado, preencher `DB_PASS`/`DB_ADMIN_PASS` nesse arquivo no servidor antes do `deploy.py up`.
